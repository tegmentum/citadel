[package]
name = "citadel-metrics-exporter"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Citadel Prometheus /metrics exporter: renders the security-state snapshot (OBS2)"

[dependencies]
citadel-otel-schema = { path = "../citadel-otel-schema" }
