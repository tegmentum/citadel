[package]
name = "citadel-otel-schema"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Citadel observability schema: canonical metric names + OTel attribute keys (OBS1)"

[dependencies]
citadel-mesh = { path = "../citadel-mesh" }
